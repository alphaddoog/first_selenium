## Copy this file to `.env.test` for local runs.
## NEVER commit `.env.test` with real credentials.

# --- CRM target ---
CRM_BASE_URL="https://your-crm.example.com"

# Optional: if your CRM requires login via UI
CRM_USERNAME="user@example.com"
CRM_PASSWORD="change-me"

# Optional: assertions for smoke test
CRM_EXPECTED_TITLE_CONTAINS="Login"

# --- Selenium ---
# When running with docker-compose, this should be:
# SELENIUM_REMOTE_URL="http://selenium:4444/wd/hub"
SELENIUM_REMOTE_URL="http://localhost:4444/wd/hub"
SELENIUM_HEADLESS="1"

# --- Test run metadata ---
RUN_ID="local"

